import React, { Component } from 'react';
import styled from 'styled-components';

export const KonuxButton = styled.button`
  cursor: pointer;
  font-size: 1 rem;
`;

KonuxButton.displayName = 'KonuxButton';
